{
  "name": "mono",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "boo": "lerna bootstrap",
    "dev:all": "lerna run dev --stream  --parallel",
    "dev:all:ios": "echo \"ios not now(can run from package\"",
    "sb:web": "lerna run --scope @mono/ui-components sb:web",
    "sb:android": "lerna run --scope @mono/ui-components sb:android"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {}
}
